@model Transferencia_Datos.Administrador_DTO.ObtenerPorId_DTO

@{
    ViewData["Title"] = "REGISTROS";
}

<style>
    /* Estilos Del Cuadro */
    .form-container {
        max-width: 650px;
        padding: 20px;
        background-color: #f7f7f7;
        border-radius: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin: auto;
        margin-top: 20px;
    }

        /* Estilos Para h1 */
        .form-container h1 {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #333;
            text-transform: uppercase;
        }

        .form-container .row {
            margin-bottom: 10px;
        }

        .form-container label {
            font-weight: bold;
        }
</style>


<!-- CUERPO HTML -->
<div class="form-container">
    <h1>Empleados Registrados</h1><br />


    <div class="table-responsive">

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("NOMBRE")
                    </th>
                    <th>
                        @Html.DisplayName("EMAIL")
                    </th>
                    <th>
                        @Html.DisplayName("CONTRASEÑA")
                    </th>
                    <th>
                        @Html.DisplayName("ROL")
                    </th>

                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Lista_Administradores)
                {
                    <tr>
                        <!-- ATRIBUTOS -->
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Contraseña)
                        </td>
                        <td>
                        @Html.DisplayFor(modelItem => item.Rol)
                        </td>



                        <!-- BOTONES -->
                        <td>
                            <div class="d-flex flex-wrap">
                                <div class="btn-group">
                                    <a asp-action="Edit" asp-route-id="@item.IdAdmin" class="btn btn-primary rounded">Editar</a>
                                    <a asp-action="Details" asp-route-id="@item.IdAdmin" class="btn btn-secondary rounded">Detalles</a>
                                    <a asp-action="Delete_Vista" asp-route-id="@item.IdAdmin" class="btn btn-danger rounded">Eliminar</a>
                                </div>
                                <style>
                                    .btn-group {
                                        display: flex;
                                        gap: 5px;
                                    }

                                    .btn {
                                        flex: 1;
                                        border-radius: 5px;
                                    }
                                </style>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>

</div>